import 'package:flutter/material.dart';

class BookAppointmentViewBodyIdentity extends StatelessWidget {
  const BookAppointmentViewBodyIdentity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Book Appointment",
      style: TextStyle(
        fontWeight: FontWeight.bold,
        fontSize: 28,
      ),
    );
  }
}
